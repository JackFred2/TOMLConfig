package red.jackf.tomlconfig.parser.token;

public class KeyJoinToken extends Token {

    public KeyJoinToken(int index) {
        super(index);
    }

    @Override
    public String toString() {
        return "KeyJoinToken";
    }
}
